# Doesn't have to be ordered in Python,
# but that's technically not a tuple.
# https://en.wikipedia.org/wiki/Tuple
# Wikipedia calls this type of tuple a "record."
tupleExample = (2, 1, 4, 3, 5)
print(tupleExample)

# This will cause an error
# tupleExample[0] = 1
# print(tupleExample)